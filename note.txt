// HubSpot can detect:
if (visit_count > 100 in 1 day) → FLAG "Bot"
if (avg_time_per_page < 5 seconds) → FLAG "Scraper"  
if (no_mouse_events) → FLAG "Automation"
if (sequential_page_pattern) → FLAG "Crawler"
// Evergage's/Salesforce ML can detect:
- Unnatural click patterns
- Bot-like timing precision
- Missing interaction events
- Unusual session durations
// Tools like FullStory track:
- Every mouse movement
- Every click
- Every scroll
- Keyboard inputs
- Form interactions
- VIDEO: Full session replay!

⚠️ If enabled: They can literally WATCH you!


----
Platform Type: Enterprise Learning Management
Security Level: VERY HIGH (Education + paid content)
Detection Capability: ADVANCED (6 tracking systems!)
Bot Detection: ML-powered (HubSpot + Evergage)
Account Linking: STRONG (email + visitor ID + session)